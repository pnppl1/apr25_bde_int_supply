# Data Modelling

Organize the first type of data into a relational database. It will therefore be necessary to think about how to segment the data into several tables.

Implement a document-oriented database with the comments. The goal would be to implement a Kibana Dashboard that allowed querying on these comments and having a "tracker" of the company that was commented on.

To hand in :
A relational database
UML Diagram 
A file who creates and queries the SQL database .
Same files for a Elastic/Mongo/ DataBase
